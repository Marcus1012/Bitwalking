<resources>



    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">-->
    <style name="MyBwAppTheme" parent="Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for coloring the Toolbar -->
        <item name="colorPrimary">#161618</item>

        <!-- colorPrimaryDark is used for coloring the status bar -->
        <item name="colorPrimaryDark">#000000</item>

        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <item name="colorAccent">#161618</item>
        <!--<item name="android:datePickerStyle">@android:style/Widget.DatePicker</item>-->
        <item name="android:datePickerStyle">@style/MyDatePicker</item>
    </style>



    <style name="WhiteActionBar" parent="Theme.AppCompat.Light">
        <item name="android:background">@color/white_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="WhiteActionBarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:actionBarStyle">@style/WhiteActionBar</item>
    </style>

    <style name="Theme.Transparent" parent="MyBwAppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>


    </style>

</resources>
